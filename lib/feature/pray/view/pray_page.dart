import 'package:flutter/material.dart';

class PrayPage extends StatelessWidget {
  const PrayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Pray Page')));
  }
}
